{
  "name": "deepstream",
  "version": "1.0.0",
  "private": true,
  "description": "DeepStream — Live sports streaming",
  "scripts": {
    "dev": "npx wrangler pages dev .",
    "deploy": "npx wrangler pages deploy ."
  }
}
