{
  "name": "deepstream",
  "version": "1.0.0",
  "private": true,
  "description": "DeepStream — Live sports streaming on Cloudflare Workers",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  }
}
